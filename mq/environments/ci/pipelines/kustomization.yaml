# The mq-qm-dev pipeline and its task library, vendored + modernized in
# ibmclientengineering/mq-infra (tekton/). Referenced remotely so the factory
# has one source of truth; bump ref to a tag/sha to pin a factory release.
apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
resources:
  - https://github.com/ibmclientengineering/mq-infra//tekton?ref=master
