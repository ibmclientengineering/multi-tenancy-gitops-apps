apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: ace-bar-store
  namespace: tools
spec:
  accessModes:
    - ReadWriteMany
  resources:
    requests:
      storage: 1Gi
  storageClassName: ocs-storagecluster-cephfs
